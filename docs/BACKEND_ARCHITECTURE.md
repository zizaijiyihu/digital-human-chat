# 后端架构设计（最终版）

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    客户端（前端/第三方应用）                  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP/WebSocket
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   Agent 应用层（对外服务）                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  对外接口（3类）:                                            │
│    1. 聊天接口 - /api/chat                                  │
│    2. 学生信息查询 - /api/student/*                          │
│    3. 知识信息查询 - /api/knowledge/*                        │
│                                                             │
│  核心功能:                                                   │
│    • 动态 Prompt 构建（学生信息 + 知识点库）                  │
│    • 会话管理（多轮对话记忆）                                │
│    • Actions 生成与解析                                     │
│    • 输出：语音 + actions                                   │
│                                                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ 内部调用（不对外）
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                 多模态引擎（内部工具模块）                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  内部工具函数:                                               │
│    • multimodal_chat() - 视频/音频/图像理解                 │
│    • stream_tts() - 流式 TTS 合成                           │
│    • text_chat() - 纯文本对话                               │
│                                                             │
│  封装:                                                       │
│    • OpenAI SDK (Qwen3-Omni-Flash)                         │
│    • DashScope SDK (Qwen3-TTS-Flash)                       │
│                                                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ API 调用
                         ↓
┌─────────────────────────────────────────────────────────────┐
│              阿里云 DashScope API（Qwen 模型）                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 对外接口（Agent 应用层）

### 1. 聊天接口

#### 1.1 多模态对话接口（主接口）

```http
POST /api/chat
Content-Type: multipart/form-data

参数:
- student_id: 学生 ID（必需）
- session_id: 会话 ID（可选，不传则自动生成）
- video: 视频文件（可选）
- audio: 音频文件（可选）
- image: 图像文件（可选）
- text: 文本消息（可选）

返回（流式）:
[4字节长度][元数据 JSON][音频流...]

元数据格式:
{
  "type": "metadata",
  "message": "你的演讲很好，但可以改进眼神交流...",
  "actions": [
    {
      "type": "show",
      "content": {
        "type": "video",
        "url": "https://cdn.example.com/eye-contact-tutorial.mp4",
        "title": "眼神交流技巧教学"
      }
    },
    {
      "type": "progress_update",
      "data": {
        "skill": "eye_contact",
        "score": 6.5,
        "improvement": "+1.2"
      }
    }
  ],
  "session_id": "session_1234567890_abc",
  "student_id": "student_001"
}
```

**Actions 类型定义：**

```typescript
// 1. show - 展示教学内容
{
  type: "show",
  content: {
    type: "video" | "image" | "ppt" | "text",
    url: string,
    title: string,
    description?: string
  }
}

// 2. progress_update - 更新学习进度
{
  type: "progress_update",
  data: {
    skill: string,           // 技能标识
    score: number,           // 当前得分
    improvement: string,     // 改进幅度 (如 "+1.2")
    timestamp: string
  }
}

// 3. progress_query - 查询历史表现
{
  type: "progress_query",
  skill: string,             // 要查询的技能
  timerange: string          // 时间范围
}

// 4. open_self_observation - 打开自我观察窗口
{
  type: "open_self_observation",
  video_segment: {
    start: string,           // "00:15"
    end: string,             // "00:32"
    highlight: string,       // 注意事项
    comparison?: {           // 可选：对比视频
      url: string
    }
  }
}

// 5. next_exercise - 推荐下一个练习
{
  type: "next_exercise",
  exercise: {
    id: string,
    title: string,
    description: string,
    duration: number         // 秒
  }
}

// 6. summarize - 生成总结报告
{
  type: "summarize",
  report: {
    overall_score: number,
    strengths: string[],
    improvements: string[],
    next_steps: string[]
  }
}
```

---

### 2. 学生信息查询接口

#### 2.1 获取学生信息

```http
GET /api/student/{student_id}

返回:
{
  "success": true,
  "student": {
    "student_id": "student_001",
    "name": "张三",
    "age": 28,
    "level": "初级",
    "background": "企业管理者，需要提升公众演讲能力",
    "goals": [
      "克服紧张情绪",
      "提升表达清晰度"
    ],
    "history": {
      "total_sessions": 15,
      "total_duration": 7200,
      "last_session": "2025-11-10T15:30:00Z",
      "strengths": ["声音洪亮", "逻辑清晰"],
      "weaknesses": ["眼神交流不足", "手势僵硬"],
      "progress": {
        "eye_contact": {"score": 6.5, "trend": "+1.2"},
        "body_language": {"score": 5.8, "trend": "+0.5"},
        "voice_control": {"score": 8.2, "trend": "+0.3"}
      }
    }
  }
}
```

---

### 3. 知识信息查询接口

#### 3.1 获取知识点列表

```http
GET /api/knowledge?category=speech_training

返回:
{
  "success": true,
  "total": 50,
  "items": [
    {
      "id": "knowledge_001",
      "topic": "eye_contact",
      "title": "眼神交流技巧",
      "description": "如何通过眼神交流增强演讲效果",
      "difficulty": "初级"
    },
    {
      "id": "knowledge_002",
      "topic": "body_language",
      "title": "肢体语言运用",
      "description": "演讲中的手势和姿态技巧",
      "difficulty": "中级"
    }
  ]
}
```

#### 3.2 获取知识点详情

```http
GET /api/knowledge/{topic}

返回:
{
  "success": true,
  "knowledge": {
    "topic": "eye_contact",
    "title": "眼神交流技巧",
    "content": {
      "theory": "眼神交流的理论基础...",
      "methods": [
        {
          "name": "3-5秒法则",
          "description": "与每位观众保持3-5秒的眼神接触",
          "example": "..."
        }
      ],
      "common_mistakes": ["盯着同一个人看太久", "完全不看观众"],
      "practice_tips": ["每天对着镜子练习", "录制视频回看"]
    },
    "resources": [
      {
        "type": "video",
        "url": "https://cdn.example.com/eye-contact-tutorial.mp4",
        "title": "眼神交流教学视频",
        "duration": 180
      }
    ],
    "difficulty": "初级"
  }
}
```

---

## 🔨 内部工具（多模态引擎）

**不对外暴露接口，仅作为内部 Python 模块使用**

### 工具函数

```python
# multimodal_engine.py

def multimodal_chat(messages, modalities=['text'], stream=False):
    """
    多模态对话

    参数:
        messages: 消息列表，支持 text/video_url/audio_url/image_url
        modalities: 输出模态 ['text'] 或 ['text', 'audio']
        stream: 是否流式返回

    返回:
        response: AI 响应内容
    """
    pass

def stream_tts(text, voice='Cherry', language='Chinese', format='pcm'):
    """
    流式 TTS 合成

    参数:
        text: 要合成的文本
        voice: 声音类型
        language: 语言
        format: 音频格式

    返回:
        generator: 音频流生成器
    """
    pass

def text_chat(messages, stream=False):
    """
    纯文本对话（更快）

    参数:
        messages: 消息列表
        stream: 是否流式返回

    返回:
        response: AI 响应内容
    """
    pass
```

### 使用示例

```python
# 在 Agent 应用层中使用
from multimodal_engine import multimodal_chat, stream_tts

# 视频理解
response = multimodal_chat(
    messages=[
        {'role': 'system', 'content': system_prompt},
        {'role': 'user', 'content': [{'type': 'video_url', 'video_url': {...}}]}
    ],
    modalities=['text']
)

# TTS 合成
audio_stream = stream_tts(text=response.content)
for chunk in audio_stream:
    yield chunk
```

---

## 📊 完整数据流

```
客户端上传视频 + student_id
    ↓
POST /api/chat
    ↓
Agent 应用层处理:
    │
    ├─ 1. 获取学生信息
    │      student = get_student(student_id)
    │
    ├─ 2. 根据当前话题获取知识点
    │      knowledge = get_knowledge(topic)
    │
    ├─ 3. 获取会话历史
    │      history = get_conversation_history(session_id)
    │
    ├─ 4. 动态构建 System Prompt
    │      system_prompt = build_prompt(
    │          template=PROMPT_TEMPLATE,
    │          student_info=student,
    │          knowledge=knowledge
    │      )
    │
    ├─ 5. 调用多模态引擎（内部工具）
    │      response = multimodal_chat(
    │          messages=[system_prompt, ...history, current_video]
    │      )
    │
    ├─ 6. 解析 JSON 响应
    │      message = response['message']
    │      actions = response['actions']
    │
    ├─ 7. 生成元数据块
    │      metadata = {
    │          'type': 'metadata',
    │          'message': message,
    │          'actions': actions
    │      }
    │      yield [长度][metadata JSON]
    │
    ├─ 8. 调用 TTS 引擎（内部工具）
    │      audio_stream = stream_tts(message)
    │      for chunk in audio_stream:
    │          yield chunk
    │
    └─ 9. 保存对话历史
           save_history(session_id, user_msg, ai_msg)
    ↓
返回给客户端：[元数据块][音频流]
```

---

## 📁 代码结构

```
digital-human-chat/
├── app.py                          # Flask 主应用（Agent 应用层）
├── multimodal_engine.py            # 多模态引擎（内部工具）
├── config.py                       # 配置文件
├── models/
│   ├── student.py                  # 学生数据模型
│   ├── knowledge.py                # 知识点数据模型
│   └── conversation.py             # 会话数据模型
├── services/
│   ├── student_service.py          # 学生信息服务
│   ├── knowledge_service.py        # 知识点服务
│   ├── conversation_service.py     # 会话管理服务
│   └── prompt_builder.py           # Prompt 构建服务
├── routes/
│   ├── chat.py                     # 聊天接口路由
│   ├── student.py                  # 学生接口路由
│   └── knowledge.py                # 知识接口路由
├── templates/
│   └── system_prompt_template.md   # System Prompt 模板
└── static/
    └── ...                         # 前端资源（可选）
```

---

## 🎯 核心设计要点

### 1. 多模态引擎 = 内部工具
- **不对外暴露接口**
- 封装成 Python 模块/函数
- 只被 Agent 应用层内部调用
- 提供统一的函数接口

### 2. Agent 应用层 = 对外服务
- **3 类对外接口**：
  1. 聊天接口（`/api/chat`）
  2. 学生信息查询（`/api/student/*`）
  3. 知识信息查询（`/api/knowledge/*`）

### 3. 动态 Prompt 构建
```python
system_prompt = 固定模板 + 动态学生信息 + 动态知识点
```

### 4. 输出格式
- **语音**：流式 PCM 音频
- **Actions**：结构化指令（6种类型）
- **传输**：元数据块 + 音频流

---

## 📋 对外接口总结

| 类别 | 接口 | 方法 | 路径 |
|------|------|------|------|
| **聊天** | 多模态对话 | POST | `/api/chat` |
| **学生** | 获取学生信息 | GET | `/api/student/{id}` |
| **知识** | 获取知识点列表 | GET | `/api/knowledge` |
| **知识** | 获取知识点详情 | GET | `/api/knowledge/{topic}` |

**共 4 个对外接口**（只读查询）

---

## 🚀 实现优先级

### Phase 1: 核心功能（MVP）
1. ✅ 多模态引擎（内部工具）
   - multimodal_chat()
   - stream_tts()
2. ⭕ 聊天接口
   - POST /api/chat（输出：语音 + actions）
3. ⭕ Mock 学生信息和知识点
   - 内存字典存储
   - 只读查询接口

### Phase 2: 数据管理
1. 学生信息查询接口（只读）
2. 知识点查询接口（只读）
3. 持久化存储（PostgreSQL/Redis）

### Phase 3: 优化
1. 会话管理优化
2. Actions 执行反馈
3. 性能优化和缓存
